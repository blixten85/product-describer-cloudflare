{
  "name": "product-describer-sync",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "secret:set-scraper-key": "wrangler secret put SCRAPER_API_KEY",
    "secret:set-anthropic-key": "wrangler secret put ANTHROPIC_API_KEY",
    "secret:set-openai-key": "wrangler secret put OPENAI_API_KEY",
    "secret:set-gemini-key": "wrangler secret put GEMINI_API_KEY"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.0.0"
  }
}
